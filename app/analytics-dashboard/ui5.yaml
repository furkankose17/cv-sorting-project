specVersion: "3.0"
metadata:
  name: cv-sorting-analytics-dashboard
type: application
framework:
  name: SAPUI5
  version: "1.120.0"
  libraries:
    - name: sap.m
    - name: sap.ui.core
    - name: sap.ui.layout
    - name: sap.f
    - name: sap.suite.ui.microchart
    - name: sap.viz

builder:
  bundles:
    - bundleDefinition:
        name: cv/sorting/analytics/Component-preload.js
      bundleOptions:
        optimize: true
  customTasks:
    - name: ui5-task-zipper
      afterTask: generateComponentPreload
      configuration:
        archiveName: cv-sorting-analytics-dashboard

server:
  customMiddleware:
    - name: fiori-tools-proxy
      afterMiddleware: compression
      configuration:
        ignoreCertError: false
        backend:
          - path: /api
            url: http://localhost:4004
    - name: fiori-tools-appreload
      afterMiddleware: compression
      configuration:
        port: 35729
        path: webapp
        delay: 300
